export enum Routing {
    Homepage = "",
    Admin = "admin",
    GameList = "gameList",
    Game = "game",
    Waiting = "waiting",
}

export enum RoutingGameMatchId {
    Solo = "solo",
    Duel = "duel",
}
