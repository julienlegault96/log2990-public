import { GameErrorDirective } from "./game-error.directive";

describe("GameErrorDirective", () => {
  it("should create an instance", () => {
    const directive = new GameErrorDirective();
    expect(directive).toBeTruthy();
  });
});
