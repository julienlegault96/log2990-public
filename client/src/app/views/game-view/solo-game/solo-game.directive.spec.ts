import { SoloGameDirective } from './solo-game.directive';

describe('SoloGameDirective', () => {
  it('should create an instance', () => {
    const directive = new SoloGameDirective();
    expect(directive).toBeTruthy();
  });
});
